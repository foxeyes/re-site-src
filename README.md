## Uploadcare future blog

### Installation:

`git clone git@github.com:uploadcare/experimental-blog.git ./`

`npm run prepare`

### Workflow:

Start local server:
`npm run dev`

Build site:
`npm run build`
